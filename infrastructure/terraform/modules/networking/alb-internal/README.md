# DEPRECATED

Use `networking/alb-api` instead.

The internal ALB model assumed CHT would call a private URL. **CHT always uses the public `MEDIAHUB_BASE_URL`** (API key auth). See [docs/mediahub-architecture.md](../../../../docs/mediahub-architecture.md).
