# DEPRECATED

Use `networking/alb-api` instead.

The internal ALB model assumed CHT would call a private URL. **CHT always uses the public `CONTENTHUB_BASE_URL`** (API key auth). See [docs/engineering/colocated-deployment.md](../../../../docs/engineering/colocated-deployment.md).
