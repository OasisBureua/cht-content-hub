# S3 public assets (KOL headshots)

Creates `{contenthub-dev-assets-{account_id}}` with public `GetObject` on `kol-headshots/*`.

**photo_url format:** `{kol_headshots_base_url}/{slug}.png`

Upload: `./scripts/migrate-kol-headshots.sh`
