# infra/modules/step-functions/

Step Function state machine module. Wraps a JSON ASL definition with an IAM execution role and CloudWatch logging. Inputs: ASL definition path, role permissions. Outputs: state machine ARN.
