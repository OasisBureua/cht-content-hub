# infra/modules/security/iam/

Project-scoped IAM roles and policies. All named `cht-content-hub-*`.

Includes: ECS task execution roles, ECS task roles, Lambda execution roles per function category, cross-service roles.
