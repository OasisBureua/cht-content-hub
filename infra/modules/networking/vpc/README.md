# infra/modules/networking/vpc/

VPC module — either creates a new VPC for `cht-content-hub` or wraps a data source reference to `cht-platform-tool`'s existing VPC.

Decision pending — see open question on VPC strategy in the implementation plan.
