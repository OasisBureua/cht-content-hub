# docs/architecture/

Architecture decision records.

ADR format: title, status, context, decision, consequences. One file per decision, numbered sequentially.

High-level migration architecture lives in Confluence. This directory holds the granular per-decision records that accumulate as the system evolves.
